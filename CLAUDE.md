# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is empty apart from `README.md` (which contains only the title `# Agent`) and this file. There is no source code, build system, dependency manifest, test suite, or CI configuration yet.

There is therefore nothing to document about architecture or commands. When the first real code lands, replace this section with:

- **Commands** — how to install dependencies, build, lint, run the app, run the full test suite, and run a single test.
- **Architecture** — the big-picture structure that is not obvious from reading any one file: the main components, how they communicate, and where the important boundaries are.

Do not treat the absence of tooling as a convention to preserve: pick the stack that fits the task, and record the resulting commands here.
